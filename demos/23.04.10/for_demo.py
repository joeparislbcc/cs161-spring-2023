#!/usr/bin/env python3
meta_vars = ["spam", "ham", "eggs"]

# var = 0
# while var < len(meta_vars):
#     print(meta_vars[var].capitalize())
#     var += 1

for var in meta_vars:
    print(var.capitalize())

print("All done!")
